using UnityEngine;
using TMPro;

// Scene references rendered by the online mode rules.
public class MatchHUD : MonoBehaviour
{
    public TMP_Text timerText, rankingText, throwableText, healthText, staminaText;
    public GameObject endPanel;
    public TMP_Text resultText;
}
